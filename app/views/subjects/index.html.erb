<div class="container">
  <%= render partial: "shared/component/heading" %>
  <div class="row content-area">
    <div class="main-container col-md-8">
      <div class="index-subject-pager">
        <h1>お題一覧(新着順)</h1>
        <%= paginate(@subjects) %>
      </div>
      <%= render partial: "shared/subject_index_container", collection: @subjects %>
      <%= paginate(@subjects) %>
    </div>
    <div class="side-container col-md-4">
      <%= render partial: "shared/right_side_container" %>
    </div>
  </div>
</div>
